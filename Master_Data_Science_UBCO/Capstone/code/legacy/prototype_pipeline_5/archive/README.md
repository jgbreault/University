# Pipeline 5 Archive

Files under `legacy_notebooks/` are retained for audit and comparison only.

They include the earlier canonical-grid notebook and the two stage-specific demo
notebooks used while developing the visual-block workflow. Do not use them as public
entry points.

Run the current pipeline from:

```text
../pipeline5_rule_extraction.ipynb
```
